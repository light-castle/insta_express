<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Posts</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
  <div class="container py-3">
    <header>
      <div class="d-flex flex-column flex-md-row align-items-center pb-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center link-body-emphasis text-decoration-none">
          <span class="fs-4">instagram</span>
        </a>
        <nav class="d-inline-flex mt-2 mt-md-0 ms-md-auto">
          <!-- '게시물 작성' 버튼 -->
          <a class="py-2 link-body-emphasis text-decoration-none" href="#"><button type="button" class="btn btn-primary" onclick="window.location.href='/posts/create'">게시물 작성</button></a>
          <!-- '친구 추가' 버튼 -->
          <a class="py-2 link-body-emphasis text-decoration-none" href="#"><button type="button" class="btn btn-primary" onclick="window.location.href='/auth/add-friend'">친구 추가</button></a>
        </nav>
      </div>

      <!-- 환영 메시지와 친구 목록 -->
      <div class="pricing-header p-3 pb-md-4 mx-auto text-center">
        <h1 class="display-4 fw-normal text-body-emphasis">환영합니다. <%= user.username %>님!</h1>
        <p class="fs-5 text-body-secondary">친구 목록: <% friends.forEach(friend => { %> <%= friend.username %>, <% }) %> </p>
      </div>
    </header>

    <main>
      <!-- 각 게시물에 대한 카드 -->
      <% posts.forEach(post => { %>
        <div class="card shadow-sm mb-3">
          <img src="<%= post.imageUrl %>" class="card-img-top" alt="Post Image">
          <div class="card-body">
            <p class="card-text"><%= post.caption %></p>
            <div class="d-flex justify-content-between align-items-center">
              <div class="btn-group">
                <!-- 'Edit' 버튼 -->
                <button type="button" class="btn btn-sm btn-outline-secondary" onclick="window.location.href='/posts/edit/<%= post._id %>'">Edit</button>
                <!-- 'Delete' 버튼 -->
                <form action="/posts/delete/<%= post._id %>" method="POST" style="display: inline;">
                  <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                </form>
              </div>
              <small class="text-body-secondary"><%= post.user.username %>님의 게시물</small>
            </div>
          </div>
        </div>
      <% }) %>
    </main>

    <footer class="pt-4 my-md-5 pt-md-5 border-top">
      <div class="row">
        <div class="col-12 col-md">
          <small class="d-block mb-3 text-body-secondary">© 조희성 웹프로그래밍 기말프로젝트 2024</small>
        </div>
      </div>
    </footer>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
</body>

</html>
